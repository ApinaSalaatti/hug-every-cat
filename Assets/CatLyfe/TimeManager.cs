using UnityEngine;
using System.Collections;

// A class that manages the updates of objects that should be pausable
public class TimeManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
